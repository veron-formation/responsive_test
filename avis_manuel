<style>
/* -------------------------------------------------------------------
   1. STYLES GÉNÉRAUX DU CARROUSEL (HTML CLASS: .temoignages-conteneur)
   ------------------------------------------------------------------- */
.temoignages-conteneur {
    /* UTILISATION DE FLEXBOX POUR LE DÉFILEMENT HORIZONTAL */
    display: flex; 
    /* Les vignettes ne doivent pas revenir à la ligne, elles restent sur une seule ligne */
    flex-wrap: nowrap; 
    
    /* Le défilement magique ! Permet de swiper/scroller horizontalement */
    overflow-x: scroll; 
    
    /* Cache la barre de défilement pour un look plus propre sur la plupart des navigateurs */
    -ms-overflow-style: none; /* IE and Edge */
    scrollbar-width: none;  /* Firefox */
    
    gap: 30px; /* Espacement entre les vignettes */
    padding: 20px;
    margin: 0 auto;
}
/* Pour cacher la barre de défilement sur Chrome/Safari/Opera */
.temoignages-conteneur::-webkit-scrollbar {
    display: none;
}

/* -------------------------------------------------------------------
   2. STYLES DE LA VIGNETTE (HTML CLASS: .vignette-avis)
   ------------------------------------------------------------------- */
.vignette-avis {
    background-color: #f7f7f7;
    padding: 25px; 
    border-radius: 8px; 
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    text-align: center;
    
    /* DÉFINITION DE LA LARGEUR CLÉ pour le Responsive */
    /* Assure que la vignette ne prend pas toute la place, forçant le défilement */
    /* Sur grand écran : chaque vignette prend environ 30% de la largeur du conteneur */
    flex: 0 0 30%; 
    
    /* Assure une largeur minimale pour la vignette sur grand écran */
    min-width: 300px; 

    display: flex; 
    flex-direction: column;
    justify-content: space-between;
}

.vignette-avis p.texte-avis {
    font-style: italic;
    color: #333;
    margin-bottom: 15px;
}


/* -------------------------------------------------------------------
   3. MEDIA QUERY (Gestion de la Vignette sur Mobile)
   ------------------------------------------------------------------- */
@media (max-width: 768px) {
    .vignette-avis {
        /* Changement Magique pour Mobile : Chaque vignette prend 85% de l'écran */
        /* Cela laisse un petit aperçu de la vignette suivante, encourageant le swipe */
        flex: 0 0 70%;
        min-width: 0; /* Réinitialise la largeur minimale */
    }

}

/* -------------------------------------------------------------------
  4. STYLES DES NOUVEAUX ÉLÉMENTS (ÉTOILES, FONCTION, LINKEDIN)
  ------------------------------------------------------------------- */

/* Conteneur des étoiles */
.evaluation-etoiles {
    margin-bottom: 15px; /* Espace sous les étoiles */
    color: #ffc107; /* Couleur jaune doré pour les étoiles */
    font-size: 1.5em; /* Taille des étoiles */
}

/* Style de chaque étoile (si vous voulez être plus précis) */
.evaluation-etoiles .etoile {
    line-height: 1; /* Assure que les étoiles sont bien alignées */
}

/* Style de l'information sur la fonction du client */

/* Style pour séparer visuellement le texte de l'avis des informations du client */
.texte-avis.commentaire-principal {
    margin-top: 20px; /* Ajoute un espace au-dessus du commentaire */
    padding-top: 20px; /* Ajoute de l'espace pour la bordure */
    border-top: 1px solid #ddd; /* Une ligne de séparation légère */
    /* La propriété 'flex-grow: 1' ou 'margin-top: auto' est assurée par la configuration flexbox de .vignette-avis */
}


/* Style pour le nom */
.client-nom {
    font-weight: bold;
    color: #007bff; /* Bleu de l'ancien client-info */
    margin-bottom: 2px;
}

/* Style pour la fonction */
.client-fonction {
    font-size: 0.9em;
    color: #6c757d; /* Gris pour un ton secondaire */
    margin-top: 0;
    margin-bottom: 5px;
}

/* Style pour le lien LinkedIn */
.client-linkedin-lien {
    display: inline-block; /* Permet d'appliquer des marges et paddings */
    font-size: 0.85em;
    color: #007bff; /* Couleur du lien */
    text-decoration: none; /* Enlève le soulignement par défaut */
    font-weight: 500;
    padding: 5px 10px;
    border: 1px solid #007bff;
    border-radius: 4px;
    transition: background-color 0.3s;
    margin-bottom: 10px;
}

.client-linkedin-lien:hover {
    background-color: #007bff;
    color: white;
}

</style>

<section class="temoignages-conteneur">
    
    <div class="vignette-avis">
        
        <div class="client-info-bloc info-top">
            <p class="client-nom">Marine</p>
            <p class="client-fonction">Chef de Projet Digital</p>
        </div>
        
        <div class="evaluation-etoiles">
            <span class="etoile pleine">&#9733;</span>
            <span class="etoile pleine">&#9733;</span>
            <span class="etoile pleine">&#9733;</span>
            <span class="etoile pleine">&#9733;</span>
            <span class="etoile pleine">&#9733;</span>
        </div>

        <a href="https://linkedin.com/in/marine" target="_blank" class="client-linkedin-lien">
            Voir le profil LinkedIn
        </a>
        
        <p class="texte-avis commentaire-principal">"Je recommande vivement cette formation Excel. J'ai commencé avec zéro base et je maîtrise le logiciel aujourd'hui, ce qui prouve l'efficacité du programme. Le formateur est extrêmement pédagogue, rendant les concepts clairs et accessibles, et les exercices pratiques sont parfaitement adaptés pour garantir une application concrète des connaissances. Si vous cherchez un apprentissage solide et efficace d'Excel, c'est un investissement incontournable."</p>
        
    </div>
    
    </section>
